export default class Pessoa { //export -> mesma coisa que deixar a classe pública em Java

    constructor(peso,altura) { //this -> peso e altura desse objeto (instância) Pessoa
        this.peso = peso;
        this.altura = altura;
    }
}